DROP INDEX IF EXISTS training_answer_activity;
-- Optional metadata is intentionally retained: removing the topic definition
-- would make existing user-entered topic values uneditable on a downgrade.
